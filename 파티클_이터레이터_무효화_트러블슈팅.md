# Tick/BeginPlay 중 동적 컴포넌트 추가/삭제로 인한 이터레이터 무효화 크래시

## 증상

- 파티클 이펙트(Blood, 피격 이펙트 등)가 런타임에 스폰된 후 재생이 끝나면 **간헐적으로 크래시** 발생
- 디버거상 크래시 지점이 `AActor::Tick()`의 `for (UActorComponent* Comp : OwnedComponents)` 루프 내부
- 재현 조건이 불규칙하며, 파티클이 많이 스폰/소멸되는 전투 상황에서 빈번하게 발생
- 타이밍에 의존하는 문제로 판단

---

## 원인 분석

### 기존 구조

`AActor::Tick()`과 `AActor::BeginPlay()`는 매 프레임/초기화 시 자신이 소유한 `OwnedComponents` 배열을 **range-for로 직접 순회**하며 각 컴포넌트의 함수를 호출하는 구조였다.

```cpp
// [수정 전] AActor::Tick()
void AActor::Tick(float DeltaSeconds)
{
    for (UActorComponent* Comp : OwnedComponents)  // ← 원본 배열 직접 순회
    {
        Comp->TickComponent(DeltaSeconds);
    }
}

// [수정 전] AActor::BeginPlay()
void AActor::BeginPlay()
{
    for (UActorComponent* Comp : OwnedComponents)  // ← 원본 배열 직접 순회
    {
        Comp->InitializeComponent();
    }
    for (UActorComponent* Comp : OwnedComponents)  // ← 원본 배열 직접 순회
    {
        Comp->BeginPlay();
    }
}
```

### 문제 발생 흐름

#### Tick에서의 크래시 (주요 발생 경로)

```
1. 무기가 적을 타격
   → HitboxComponent::PerformSweepCheck()에서 PhysX SweepBox 히트 감지

2. 히트 시 Blood 파티클 액터를 동적 스폰
   → World->SpawnActor<AActor>()
   → 새 액터에 UAnimNotifyParticleComponent를 AddNewComponent()로 추가

3. 파티클 재생 완료 콜백에서 해당 액터를 즉시 Destroy() 호출

4. Destroy() 내부에서 OwnedComponents 배열에서 컴포넌트를 제거

5. 이 시점이 다른 액터의 Tick() 순회 도중이면
   → OwnedComponents의 내부 메모리가 재할당되거나 크기가 변경됨
   → range-for의 이터레이터가 무효화
   → 댕글링 포인터 접근
   → 크래시
```

#### BeginPlay에서의 크래시

```
1. AActor::BeginPlay()에서 OwnedComponents를 순회하며 InitializeComponent() 호출

2. 특정 컴포넌트의 InitializeComponent() 내부에서 새로운 컴포넌트를 동적으로 생성/추가
   → OwnedComponents 배열이 변경됨

3. 이후 BeginPlay() 순회에서도 동일하게 발생 가능

4. 순회 중인 배열이 변경됨
   → 이터레이터 무효화
   → 크래시
```

### 핵심 원인

C++ STL/커스텀 컨테이너의 range-for 루프는 내부적으로 `begin()`/`end()` 이터레이터를 캐싱한다. 순회 도중 배열에 `Add()`/`Remove()`가 발생하면:

- **Add**: 배열 용량 초과 시 내부 메모리 재할당 → 기존 이터레이터가 해제된 메모리를 가리킴
- **Remove**: 배열 크기 감소 → `end()` 위치가 변경되거나 원소가 이동됨

두 경우 모두 **이터레이터가 무효화되어 정의되지 않은 동작(UB)**이 발생한다.

---

## 해결

### 1단계: 복사본 순회로 이터레이터 보호

`OwnedComponents` 원본을 직접 순회하지 않고, **복사본을 만들어 순회**하도록 변경.

```cpp
// [수정 후] AActor::Tick()
void AActor::Tick(float DeltaSeconds)
{
    bool bIsEditor = World->bPie == false;

    // 복사본으로 순회 (Tick 중 컴포넌트 추가/삭제 시 이터레이터 무효화 방지)
    TArray<UActorComponent*> ComponentsCopy(OwnedComponents.begin(), OwnedComponents.end());

    for (UActorComponent* Comp : ComponentsCopy)
    {
        if (!Comp || !Comp->IsComponentTickEnabled()) continue;

        if (bIsEditor)
        {
            if (!Comp->bTickInEditor)
            {
                continue;
            }
        }

        Comp->TickComponent(DeltaSeconds);
    }
}

// [수정 후] AActor::BeginPlay()
void AActor::BeginPlay()
{
    TArray<UActorComponent*> ComponentsCopy(OwnedComponents.begin(), OwnedComponents.end());

    for (UActorComponent* Comp : ComponentsCopy)
    {
        if (Comp)
        {
            Comp->InitializeComponent();
        }
    }
    for (UActorComponent* Comp : ComponentsCopy)
    {
        if (Comp)
        {
            Comp->BeginPlay();
        }
    }
}
```

- 원본 배열이 Tick/BeginPlay 도중 변경되어도 **복사본의 이터레이터는 영향을 받지 않음**
- 복사본에 남아있지만 이미 삭제된 컴포넌트는 `if (!Comp)` null 체크로 안전하게 스킵

### 2단계: 지연 삭제(PendingKill) 패턴 도입

근본 원인인 "Tick 도중 즉시 삭제"를 방지하기 위해 **지연 삭제 시스템**을 설계.

```cpp
// AActor::Destroy() - 즉시 삭제가 아닌 지연 삭제 요청
void AActor::Destroy()
{
    // 재진입/중복 방지
    if (IsPendingDestroy())
    {
        return;
    }

    MarkPendingDestroy();
    World->AddPendingKillActor(this);  // 큐에 등록만
}
```

```cpp
// UWorld - 프레임 말미에서 안전하게 일괄 삭제
void UWorld::AddPendingKillActor(AActor* Actor)
{
    PendingKillActors.Add(Actor);
}

void UWorld::ProcessPendingKillActors()
{
    if (PendingKillActors.IsEmpty())
    {
        return;
    }

    // 파괴 목록의 사본을 만듦 (삭제 중 새로운 PendingKill 등록 대비)
    TArray<AActor*> ActorsToKill = PendingKillActors;
    PendingKillActors.Empty();

    for (AActor* Actor : ActorsToKill)
    {
        Actor->EndPlay();
        DestroyActor(Actor);
    }
}
```

```cpp
// UWorld::Tick() 끝에서 호출
void UWorld::Tick(float DeltaTime)
{
    // ... 모든 액터 Tick 처리 ...
    // ... 물리 시뮬레이션 ...

    // 지연 삭제 처리 (프레임의 가장 마지막에 실행)
    ProcessPendingKillActors();
}
```

- 모든 액터의 Tick이 끝난 후 **프레임 말미에 일괄 삭제**되므로 순회 중 배열 변경이 발생하지 않음
- `ProcessPendingKillActors()` 내부에서도 사본을 만들어 순회 → 삭제 중 새로운 PendingKill 등록에도 안전

### 3단계: 파티클 스폰 코드에 적용

파티클 재생 완료 콜백에서 `Destroy()` 직접 호출 대신 `AddPendingKillActor()`를 사용하고, `TWeakObjectPtr`로 이미 삭제된 액터에 대한 댕글링 접근도 방지.

```cpp
// HitboxComponent.cpp / Character.cpp - 파티클 스폰 시
TWeakObjectPtr<AActor> EmitterActorWeak(EmitterActor);
ParticleComp->OnParticleSystemFinished.Add(
    [EmitterActorWeak](UParticleSystemComponent* FinishedComp) {
        if (EmitterActorWeak.IsValid() && GWorld) {
            // 즉시 삭제 대신 안전한 시점에 삭제
            GWorld->AddPendingKillActor(EmitterActorWeak.Get());
        }
    }
);
```

- `TWeakObjectPtr`: 액터가 이미 다른 경로로 삭제되었을 경우 `IsValid()`가 false를 반환 → 댕글링 포인터 접근 방지
- 콜백이 Tick 도중 호출되어도 즉시 삭제가 아닌 **큐 등록만** 하므로 이터레이터에 영향 없음

---

## 적용 범위

| 적용 위치 | 수정 내용 |
|-----------|-----------|
| `AActor::Tick()` | OwnedComponents 복사본 순회 |
| `AActor::BeginPlay()` | InitializeComponent / BeginPlay 복사본 순회 |
| `AActor::Destroy()` | 즉시 삭제 → PendingKill 큐 등록으로 변경 |
| `UWorld::Tick()` | 프레임 말미에 ProcessPendingKillActors() 호출 |
| `HitboxComponent` | 파티클 완료 콜백에서 WeakPtr + PendingKill 적용 |
| `Character` | 피격 파티클 완료 콜백에서 WeakPtr + PendingKill 적용 |

---

## 요약

| 항목 | 내용 |
|------|------|
| **문제** | Tick/BeginPlay 순회 중 동적 컴포넌트 추가/삭제로 이터레이터 무효화 → 간헐적 크래시 |
| **원인** | range-for가 참조하는 원본 배열이 콜백/초기화 중 변경됨 |
| **해결** | (1) 배열 복사본 순회 (2) 지연 삭제(PendingKill) 패턴 도입 (3) TWeakObjectPtr로 댕글링 방지 |
| **결과** | 전투 중 파티클 스폰/소멸이 빈번한 상황에서도 크래시 없이 안정적으로 동작 |
